# Weight provenance and DIMER hosting

- Upstream: `Salesforce/blip-image-captioning-base`
- Immutable revision: `82a37760796d32b1411fe092ab5d4e227313294b` (the Hub's `main` resolved to this commit on 2026-09-14)
- **Two weight files upstream, no SafeTensors:** `pytorch_model.bin` (989,820,849 bytes, float32, a PyTorch pickle; SHA-256 `d6638651a5526cc2ede56f2b5104d6851b0755816d220e5e046870430180c767`, LFS `oid` confirmed by local re-hash) and `tf_model.h5` (990,275,136 bytes, the TensorFlow port; SHA-256 `d0aaa4c0e003f599d8baa53a9dee85af14eef20554cf2f8113a2673e25a59f8c` from the Hub's LFS pointer, not downloaded or re-hashed here). There is no `model.safetensors` at this revision and no conversion PR on the Hub.
- **Executed artifact ≠ hosted artifact (MODEL_ASSET_SPEC ID10).** This package executes `pytorch_model.bin` — the only PyTorch weight file — after its manifest SHA-256 is checked, with `weights_only=True`. DIMER does not accept `.bin` uploads, so the profile's hosted blob is the upstream `tf_model.h5` (an accepted format) — a different file with the same architecture and, per upstream, the same trained parameters, which this repository neither loads nor verifies against the PyTorch file (no `from_tf` path; MarianMT precedent). Both digests are recorded above and in `MODEL_CARD.md`; MODEL_ASSET_SPEC §27 conversion to `safetensors` would make the two artifacts equal and was not chosen for this wave (decision 2026-09-13).
- Manifest: `weights/blip-image-captioning-base/dimer-base-manifest.json` (8 files: `README.md`, `config.json`, `preprocessor_config.json`, `pytorch_model.bin`, `special_tokens_map.json`, `tokenizer.json`, `tokenizer_config.json`, `vocab.txt`; 990,775,593 bytes total, per-file SHA-256). `tf_model.h5` is deliberately not listed.
- Upstream weight license: BSD-3-Clause (the checkpoint's `README.md` front matter and the Hub's licence tag; the upstream code repository `salesforce/BLIP` carries the same licence)
- DIMER hosting: BSD-3-Clause permits use, modification, distribution, and commercial use subject to preservation of the copyright notice and disclaimer and to not using the Salesforce name for endorsement. The Git repository does not vendor either weight file (`weights/**/*.bin` and `weights/**/*.h5` are git-ignored); DIMER may mirror the upstream `tf_model.h5` in its model store under the upstream license.
- Fresh clone: `stage_missing_files(allow_download=True)` fetches only the manifest-listed files absent on disk, at the pinned revision, into the snapshot directory; `verify_snapshot()` then checks every file before any load. `weights/**` is marked `-text` in `.gitattributes` so Windows `core.autocrlf` cannot rewrite the committed small files and break their digests.
- Loader trust boundary (pickle checkpoint): `verify_snapshot` re-hashes `pytorch_model.bin` against the manifest **before** `torch` or `transformers` is imported, so a modified pickle never reaches a deserialiser; then `BlipForConditionalGeneration.from_pretrained(<verified dir>, use_safetensors=False, weights_only=True, trust_remote_code=False, local_files_only=True)` makes `transformers` call `torch.load(..., weights_only=True)`, whose restricted unpickler admits tensors, primitives and containers only. The card-pass smoke wrapped `torch.load` and observed exactly two calls, both on `pytorch_model.bin` with `weights_only=True`; the BERT tokenizer (`vocab.txt`, `tokenizer.json`) comes from the same snapshot; nothing model-related is fetched at construction or inference (the smoke run loaded and ran with `HF_HUB_OFFLINE=1`).
